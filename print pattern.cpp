#include<stdio.h>
main()
{
	int rows,coef=1,i,j;
    printf("\nenter the number of rows:");
    scanf("%d",&rows);
    for(i=o;i<rows;i++)
	{
		for(space=1;space<=rows-i;space++)
		printf("\n");
		for(j=0;j<i;j++)
		{
			if(j=0;j<=i;j++)
			coef=1;
			else
			coef=coef*(i-j+1)/j;
			printf("%d",coef);
		}
		printf("\n");
		
	}
	return 0;
}

